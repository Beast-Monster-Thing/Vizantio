# NationStates stat priority list
# Format: Stat Name — score
# Positive score = want this stat HIGHER
# Negative score = want this stat LOWER
# Absolute value determines priority weight
# Stats not listed here are ignored entirely
# Example entries — edit to match your nation's goals:
# Religiousness —
# Defense Forces —
# Law Enforcement —
# Authoritarianism —
# Compliance —
# Taxation —
# Government Size —
# Influence —
# Social Conservatism —
# Arms Manufacturing —
# Ideological Radicality —
# Weaponization —
# Information Technology —
# Culture —
# Intelligence —
# Economy —
# Agriculture —
# Population —
# Manufacturing —
# Book Publishing —
# Scientific Advancement —
# Mining —
# Employment —
# Health —
# Political Apathy —
# Ignorance —
# Foreign Aid —
# Wealth Gaps —
# Average Income of Rich —
# World Assembly Endorsements —
# Integrity —
# Residency —
# Public Education —
# Automobile Manufacturing —
# Tourism —
# Civil Rights —
# Political Freedom —
# Secularism —
# Freedom From Taxation —
# Pacifism —
# Inclusiveness —
# Economic Freedom —
# Youth Rebelliousness —
# Black Market —
# Crime —
# Recreational Drug Use —
# Gambling —
# Nudity —
# Corruption —
# Income Equality —
# Eco-Friendliness —
